# TOSS_REDESIGN_SPEC.md — 제로베이스 IA 재설계 (2026-09-03)
Reference: 토스뱅크 홈(계좌 리스트), 분석/보험 화이트스페이스, 다크 금융 그래프, 결제 바텀시트.
원칙: 화면당 주인공 1명 · 완료된 정보는 접는다 · 카드는 최후수단 · 블루 다이어트 · 리스트 우선.

## 섹션별 판정 (KEEP/MOVE/MERGE/COLLAPSE/REMOVE/REDESIGN)

### 홈 (제로베이스 → STATE-DRIVEN)
| 기존 | 판정 | 근거 |
|---|---|---|
| 진행 카드(50/75% 바+체크리스트 4줄) | **REMOVE** | 완료된 계정생성·본인확인이 히어로 강탈. 진행감은 히어로 eyebrow 한 줄로 압축 |
| "이제 1분이면 돼요"+CTA (카드 안) | **REDESIGN** | 카드 제거 → 페이지 레벨 히어로(큰 타이포+단일 CTA). Toss 분석 화면의 whitespace |
| 예상 페이백 확인 카드 | **REDESIGN** | 카드 → 리스트 행(chevron) |
| 잔고+보너스+거래소+출금 (메가카드 내부) | **REDESIGN** | 잔고=히어로 금액(amt-hero), 거래소·출금·알림=리스트 행. 카드 경계 제거 |
| 예측 500 USDT 카드 | **COLLAPSE** | 리스트 행 한 줄("매달 500 USDT 예상") |
| 알림 카드 | **REDESIGN** | 리스트 행 + 스위치 컴포넌트 |
| 실시간 테이블(4컬럼+헤더+완료×5) | **REDESIGN** | 어드민 테이블 → 리스트 행(아이콘+UID+금액+시각). 헤더·상태 컬럼 제거 |
| 블루 eyebrow·채널보기 블루·완료 블루점 | **REMOVE** | 블루는 primary CTA·%배지·연결 상태만 |
| "쌓인 페이백을 한 푼도…" 마케팅 서브 | **REMOVE** | §22 카피 다이어트 |

### 홈 상태 매트릭스 (히어로가 상태마다 바뀜)
- **NEW**: eyebrow "가입 완료 · 연결만 남았어요" → H1 "이제 1분이면 돼요" → sub(30 USDT 골드) → CTA 계정 연결하기 → [예상 페이백 행] → 실시간
- **RESUMABLE**: eyebrow "잠깐 멈췄던 곳" → H1 "이어서 하기" → sub 남은 단계 → CTA
- **LINKED**: 히어로=잔고 30.00 USDT + "가입 보너스가 들어왔어요" → 주인공 액션 "거래하고 첫 페이백 받기"(primary) → 행: 연결 거래소 / 예상 500 / 출금 / 알림 스위치 → 실시간
- **ACTIVE**: 히어로=잔고 42.47 + 내역 요약 → CTA 출금하기 → 행: 최근 페이백 / 거래소 / 알림 → 실시간

### 거래소 선택 (w1·pick)
카드 그리드(셀 1개=화면 1/6) → **리스트 행**(아이콘 36 + 이름 + 우측 % 배지). 실제 TetherMax Partner Exchanges 캡처와도 일치. 스캔 속도 ↑, 탭 영역 h60.

### w2 거래 규모
카드 칩 → 리스트 행(타이틀+서브+chevron), 동일 row 시스템.

### 금융 숫자 시스템
- `.amt-hero`: 40/800 tabular + 단위 17/600 text-2 (baseline 정렬)
- `.amt-row`: 15/700 + 단위 11/400 text-3
- 히어로 금액은 화면당 1개만.

### 신규 컴포넌트
`.lrow`(아이콘·타이틀·서브·트레일링) · `.sw`(스위치) · `.hero`(페이지 히어로) · `.sec-h`(중립 섹션 헤더)

### 유지
시트 시스템(드래그·Escape) · Spring 모션 · 축하 모먼트 · 위저드 구조 · 라우터 · 데모 패널 · 실측 토큰(다크 유지, §23)

## 롤백
`git checkout checkpoint-before-toss-redesign -- tmx-onboarding/index.html`
