# TETHERMAX_UI_AUDIT.md
tethermax.io/ko·/en DOM 실측(2026-09-03) + 사용자 제공 스크린샷 9장 대조. 이 값이 design-tokens의 유일한 출처다.

## 1. Color (실측 hex)
| 토큰 | 값 | 출처 |
|---|---|---|
| bg | #121721 | --background-default 실측 |
| surface | #1D2028 | 필터칩 비선택 bg / 스탯 카드 |
| surface-2 | #232732 | 테이블 헤더 밴드(추정 보간) |
| line | rgba(255,255,255,.08) | 디바이더 |
| line-strong | rgba(255,255,255,.227) | 인풋 보더 실측 |
| text | #fff / text-2 rgba(255,255,255,.7) / text-3 rgba(255,255,255,.45~.6) | --text-* 실측 |
| brand | #2453F0 | --primary-blue-main 실측 |
| brand-deep | #0031C6 | 선택 칩·상담 버튼 실측 |
| gold(보상) | #F0B90B~#F5C36A 계열 | "10,000 USDT" 골드 카피 (스크린샷) |
| green(변화) | #00A68A | 전일대비 실측 — 음수여도 green |
| error | #F44336 / warning #FFA726 / success #66BB6A | MUI 토큰 실측 |

## 2. Typography
WantedSans(+Inter, -apple-system fallback) · base 16px
| 용도 | 값 |
|---|---|
| 히어로 | 60/700/lh66 (모바일 환산 28~32/700) |
| 섹션 타이틀 | 20/700 (모바일) |
| 본문 | 16/400/lh24/ls.15 |
| 서브·헬퍼 | 14/400 text-2 |
| 메타·상태 | 12/400 |
| 금액 강조 | 20~32/700 tabular |

## 3. Component 규격 (실측)
| 컴포넌트 | 규격 |
|---|---|
| 버튼 primary | filled #2453F0, r4, h44(내부)/h42, 15/500, pad 8×24. 히어로 CTA만 pill r1000 |
| 버튼 secondary | surface bg + line-strong 보더, r4, h44 |
| 필터칩 | pill r100, h32-34, 선택=#0031C6/white, 비선택=#1D2028/#76787E |
| 인풋 | h56, r4, 보더 rgba(255,255,255,.227), bg 지면색, 16px |
| % 배지 | bg #2453F0, r6, pad 4×6, 16/600 white |
| 카드 | surface, r12, 보더 없음(또는 line 1px) |
| 리스트 행 | h56, pad 12×8, 카드 없이 디바이더 |
| 테이블 | 둥근 컨테이너 + 헤더 그레이 밴드, 행 56-64, 12-14px |
| 상태 | 배지 아님 — '완료' 파란 점 + 파란 텍스트 12px |
| Linked UID | 거래소 아이콘 원형 + 이름 + "● UID 685432171" 회색 행 (스크린샷 실측) |
| 플로팅 지원 | 우하단 원형 파랑 헤드셋 ~52px |

## 4. 패턴 어휘
롤링 숫자 카운터(odometer) · 실시간 지급 테이블(UID 마스킹 42*****93, 시간 2026/09/03, 16:01 표기) ·
텔레그램 비행기 버튼("View live payouts") · 스텝 3단계 넘버 원 · FAQ 아코디언 ·
질문형 위저드("먼저, 가장 자주 사용하는 거래소는 어디인가요?" + 그리드 + 뒤로/다음) ·
🎁+골드 보상 카피(이모지는 보상 한정) · 카운트다운 · 발광 앱 아이콘 히어로

## 5. 표기 규칙
금액: `37.66 USDT` (소수 2자리, 단위 뒤, 콤마) · %: 정수 `77%` · 시간: `2026/09/03, 16:01` ·
UID 마스킹: 앞2+별표+뒤2 · 변화율: 괄호 `(-0.91%/전일대비)` green

## 6. 신규 조합 (테더맥스에 없는 화면 — 기존 컴포넌트 조합으로만 제작)
- 진행 박스 = 스탯 카드(r12) + 프로그래스바(브랜드 필) + 리스트 행 체크리스트
- 복귀 대기 화면(D-2) = 위저드 질문 패턴 + 버튼 2단
- 보너스 연출 = 롤링 카운터 + 🎁골드 카피
- 알림 카드 = 스탯 카드 + 상태 텍스트 패턴
모두 audit 규격 내 조합이며 신규 시각 언어 발명 없음.

## 7. Tech 주석
스택: 단일 HTML SPA(기존 산출물 관례, GitHub Pages 즉시 배포). 브리프의 "design-tokens 파일" 요건은
단일 파일 구조상 `:root` CSS 변수 블록( `/* ═ design-tokens ═ */` 섹션)이 그 역할 — 컴포넌트 CSS는
전부 var()만 참조, 색·치수 하드코딩 금지. mock은 `MockAPI` 오브젝트(service layer)로 분리,
fake delay는 그 안에서만.
