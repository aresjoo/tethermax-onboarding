# QA_LOG.md — 3회 반복 기록 (2026-09-03)
브라우저 패널이 스크린샷 캡처를 지원하지 않는 세션이라, before/after 이미지는 저장 불가.
대신 각 iteration을 **computed-style 실측 대조**(스크린샷 눈대중보다 정밀)와 실클릭 구동 로그로 기록한다.

## Iteration 1 — 토큰 실측 대조 (390×812)
| 항목 | audit 기준 | 실측 | 판정 |
|---|---|---|---|
| 앱 지면 | #121721 | rgb(18,23,33) | ✓ |
| 서피스 | #1D2028 | rgb(29,32,40) | ✓ |
| 버튼 primary | #2453F0 · r4 · h44 · 15/500 | 동일 | ✓ |
| % 배지 | #2453F0 · r6 | 동일 | ✓ |
| 인풋 | h56 · r4 · 보더 rgba(255,255,255,.227) | 동일 | ✓ |
| 상태 텍스트 | 파란 점+파란 12px | 동일 | ✓ |
| 폰트 | Wanted Sans Variable 로드 | document.fonts.check=true | ✓ |
| 가로 오버플로 | 0 | 0 | ✓ |

## Iteration 2 — 상호작용/구조 결함 수정
1. **시트 히스토리 레이스** (치명): 시트 버튼의 history.back()과 go()가 경합해
   pick 진입 직후 홈으로 튕김 → 시트를 히스토리에서 분리, 시각적 open/close로 변경. 재검증 통과.
2. **중단 복귀 소실** (치명): 홈 렌더 시 step='home' 덮어쓰기로 "이어서 하기"가 안 뜸
   → 홈 복귀 시 step 유지로 수정. 복귀 타이틀/서브/점프 재검증 통과.
3. C-2 화면의 의미 없는 "·" 플레이스홀더 → 원형 안내 아이콘으로 교체.

## Iteration 3 — 라이팅 전수 + 엣지
1. 전 화면·시트 문구 수집 → UX_WRITING_GUIDE 표와 1:1 대조, 4문항(짧은가/유저 주어/
   다음 행동/불안 제거) 전 문구 통과. 이탈 문구 0건.
2. 엣지 가드: exchange 없이 step=uid/d1로 복귀하는 경우 pick으로 강등.
3. 지급 테이블 금액에 USDT 단위 추가 (실사이트 표기 규칙 `37.66 USDT` 일치).

## Iteration 4 — 모션 전면 적용 (토스×제미나이, 2026-09-03)
스프링 엔진(semi-implicit euler, 1/120 substep, 인터럽터블) + 실측 구동 검증:
1. **성공 모먼트 5박자**: 우주 버스트 → 기프트 젤리 착지(scale 1.55→1 오버슛) → 타입 리베일 2줄
   → 슬롯 롤링 '30' 정확 착지(strips -3em/0em) → sub/CTA 스프링 페이드 ✓
2. **프로그레스 오버슛**: 50 → **79.3% 오버슛** → 75.0036% 안착 실측. %텍스트 슬롯 롤링,
   체크 아이콘 stroke 드로우(.just) ✓
3. **머니 플라이트**: 칩 top 70(380ms 앤티시페이션) → 낙하 → 465 오버슛 → 406(잔고 위치) 안착
   → 잔고 슬롯롤 42.47 + 젤리 + 토스트 ✓
4. **위저드 시네마틱**: 로딩 → 0.4s 정적 포즈(그 시점 리베일 0줄 확인) → 타입 리베일 → 슬롯 '500' ✓
5. **화면 전환**: 전환 중간 두 뷰 동시 스프링 이동(-15.7px 진입/+72.6px 퇴장) → 종료 후 인라인 스타일 클린 ✓
6. **버그 수정**: celeDone/demoPreset의 go()+render() 이중 렌더가 드로우·스프링을 지우던 문제
   → goHome() 단일 렌더로 통일. 머니칩 타깃 캡처를 라우터 렌더 후(80ms)로 이동.
7. reduced-motion: 전 경로 가드(스프링 jump, 슬롯 즉시 텍스트, CSS 미디어쿼리로 .tl/.rv 표시) — 코드 경로 검증.
   시트 드래그는 포인터 캡처 시퀀스 특성상 코드 검증(속도 0.55px/ms 또는 34% 초과 시 닫힘, 미만 시 스프링 복귀).
8. 콘솔 에러 0. 저사양 자동 다운시프트(120프레임 창에서 40+ 슬로우 → 파티클 40% 감축) 탑재.

## 실클릭 구동 로그 (전 분기)
신규 홈(50%·체크리스트4·결핍 테이블 5행) ✓ → 위저드 3스텝(11거래소·규모4·계산 1.6s·
예측 500 저장·홈 카드 치환) ✓ → ask 시트 2분기 ✓ → pick(연결/가입 모드 타이틀 분기) ✓ →
UID C-3 형식·C-3 미등록·C-2 미적용·성공(축하+30 롤링+75%) ✓ → 복귀("이어서 하기 —
Binance UID 입력만 남았어요"+새로고침 지속) ✓ → D-1 코드 복사+D-2 왕복 ✓ →
알림 허용/거부 분기 ✓ → 출금 시트(후고지) ✓ → 데모 첫 페이백(100%·+12.47) ✓ →
뒤로가기 uid→pick→home ✓ · 콘솔 에러 0 (본 페이지 발생분 기준)
